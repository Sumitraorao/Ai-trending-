import './globals.css'

export const metadata = {
  title: 'AI Agents Projects',
  description: 'Ready to use AI agents from 500-AI-Agents-Projects repository',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
